import type {
  SxProps,
  Theme,
} from "@mui/material";

export const dashboardWrapper: SxProps<Theme> =
  {
    flexGrow: 1,
    minHeight: "100vh",
  };

export const mainContainer: SxProps<Theme> =
  {
    mt: 6,
    mb: 10,
  };
